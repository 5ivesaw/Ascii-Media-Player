ASCII Media Player for Linux

Run ./install.sh to install the program for your user, or run ./ascii-media-player directly.

Examples:

  ./ascii-media-player ~/Music
  ./install.sh
  ascii-media-player ~/Music

FFmpeg is optional and is only required for conversion. Install it through your distribution package manager.
